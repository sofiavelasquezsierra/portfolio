# security tray image

Save your background-removed tray photo as `public/security-tray.png` (transparent PNG, ~880×660). Until you do, the about page falls back to a styled gray gradient bin so nothing breaks.

Tip: remove.bg or Photoshop's "Remove Background" works well on the gray plastic bin photo you uploaded.
