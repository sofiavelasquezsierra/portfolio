# Photos

Drop in:
- `sofia.jpg` — main about-page portrait (square, ~600×600 works well)
- `passport.jpg` — optional travel/passport-style image for the about page
- Anything else you want to use; reference from `/photos/<filename>` in components.

Until files exist, the about page renders styled placeholder cards.
